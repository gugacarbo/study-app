/** Raw multipart body limit (512 KB). */
export const MAX_UPLOAD_BYTES = 524_288;

/** Decoded UTF-8 text length limit after TextDecoder. */
export const MAX_TEXT_CHARS = 50_000;

/** Maximum questions persisted per ingest job. */
export const MAX_QUESTIONS = 100;
